# Components

shadcn/ui will be bootstrapped fully when needed by later chunks.
Chunk 1 uses Tailwind v4 + hand-rolled shared UI under `src/components/`
(DispatchShell, NeedsInfoBadge) with brand tokens from `src/index.css`.
